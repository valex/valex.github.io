extends basic.pug

block content

    <div class="row">
        <div class="col-sm-8">
    
            <div id="svg_chart"></div>

            <div class="alert alert-secondary rounded-0 py-2" role="alert">
                <p style="font-size: 0.8rem;" class="mb-2">Move vector by dragging circle at the end of arrow and have fun ;)</p>
            </div>

            <div class="alert alert-warning" role="alert">
                <p>The <b>length</b> of a vector is also sometimes called the <i>magnitude</i> or the <i>norm</i> of a vector.</p>
        
                <p>The length or magnitude or norm of the vector `vec a` is denoted by `norm(vec a)` or, less commonly, `abs(vec a)`</p>
                
                <p>The length of a `n`-dimensional vector `vec V` is a scalar given by the formula:</p>

                <p class="text-center">`norm(vec V) = sqrt(sum_(i=1)^n V_i^2)`</p>

                <p>The length of a `two`-dimensional vector `vec a` is a scalar given by the formula:</p>

                <p class="text-center">`norm(vec a) = sqrt(a_x^2 + a_y^2)`</p>
            </div>
        </div>

        <div class="col-sm-4">
            <div class="table-responsive">
                <table class="table">
                    <thead>
                    <tr>
                        <th></th>
                        <th class="text-center">x</th>
                        <th class="text-center">y</th>
                    </tr>
                    </thead>
                    <tbody>
                    <tr>
                        <th>`vec a`</th>
                        <td>
                        <input id="t_a_x" class="form-control form-control-sm" type="text" >
                        </td>
                        <td>
                        <input id="t_a_y" class="form-control form-control-sm" type="text" >
                        </td>
                    </tr>
                    <tr>
                        <th class="text-nowrap" colspan="2">`norm(vec a) = sqrt(a_x^2 + a_y^2)`</th>
                        <td id="t_result" class="text-center align-middle"></td>
                    </tr>
                    </tbody>
                </table>
            </div>

            include see_also.pug
            
        </div>
    </div>